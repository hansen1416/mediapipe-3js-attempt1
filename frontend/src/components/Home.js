import { useEffect, useRef } from "react";
import "./Home.css";

import Scene from "./Scene";

export default function Home() {
	useEffect(() => {}, []);

	return (
		<div className="box">
			<Scene />
		</div>
	);
}
